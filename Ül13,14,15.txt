INSERT INTO users (first_name, last_name, email, phone, image)
VALUES ('Kert', 'Poldmaa', 'kert@email.com', '56432567', 'kert.jpg');
INSERT INTO users (first_name, last_name, email, phone, image)
VALUES ('Kurt', 'Puldmaa', 'kurt@email.com', '56444567', 'kurt.jpg');

SELECT * FROM users WHERE first_name LIKE 'G%' AND email LIKE '%.com';

SELECT *, 
  CASE 
    WHEN phone LIKE '372%' THEN 'EESTI'
    ELSE '---'
  END AS eesti,
CASE
	WHEN email LIKE '%gov%' or '%edu%' THEN
	'HARIDUSASUTUS'
	ELSE '---'
  END AS asutus
FROM users

ORDER BY eesti DESC
;

UPDATE users
SET email = 'muudetud@gmail.com', phone = '123'
WHERE id = 1;

DELETE FROM users
WHERE id BETWEEN 5 AND 10;